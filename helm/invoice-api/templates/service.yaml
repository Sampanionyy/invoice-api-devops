apiVersion: v1
kind: Service
metadata:
  name: {{ include "invoice-api.fullname" . }}
  labels:
    app: {{ include "invoice-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
  selector:
    app: {{ include "invoice-api.fullname" . }}
